import Component from "$store/components/ui/MeasureTable.tsx";
import type { Props } from "$store/components/ui/MeasureTable.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
